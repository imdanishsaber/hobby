import Store from '../store';
import Family from '../models/family';
import { FamilyType } from '../types';
declare const _default: (store: Store) => (parentIDs: string[], type?: FamilyType, isMain?: boolean) => Family;
export default _default;
